name: Helios
description: ${description}
authors: [ TehBrian ]
website: https://github.com/SkyboundCity/Helios

version: ${version}
main: city.skybound.helios.HeliosPlugin
api-version: 26.2

bootstrapper: city.skybound.helios.HeliosBootstrap

dependencies:
  server:
    LuckPerms:
      load: BEFORE
      required: true
